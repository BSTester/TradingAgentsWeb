rors.     er        e:
xception as   except E   
  p(0.001)e.slee    tim           1
 rites'] += ons['wperati       o           
  ith lock:   w         ': i})
    d, {'valuet(user_ie.sech      ca         ):
 ange(20for i in r               try:
  er():
   ef writ    d  
 {e}")
  eader:append(f"Rs.    error  
      tion as e:cept Ex  excep  .001)
    me.sleep(0ti            += 1
    '] s['readsationer   op                 k:
th loc wi              id)
 er_uscache(nfig_from_ser_coget_u  config =        ):
       (20nge ra    for i iny:
         tr
       ():f reader
    de  g.Lock()
   = threadinck= []
    loerrors }
    tes': 0lida 0, 'inva'writes':reads': 0, = {'rations    opeid = 1
 )
    user_ig_cache(user_confget_e =  cach
    60)
    int("=" *pr  ")
  OperationsWrite ixed Read/3: Mt("Test 
    prin 60)"=" * n" +\print(""""
    erationste opxed read/wri""Test mi    "ixed():
_write_mef test_read")

dhread safe!rs - To erro"✅ N     print(
   
    else:{error}")"   nt(f  pri     
     ors[:5]: errror in er
        forerrors:  if )}")
  rrorsn(eErrors: {lef"✅   print()
  ies']}"ntrats['total_e users: {stf"✅ Cached print(stats()
   he.get_ stats = cac  figs
 rs have consell u  # Verify a
    
   t.join()       threads:
for t in   ads
  r all threWait fo
    #    tart()
 
        t.spend(t)eads.ap        thr{i}"))
(i, f"user_ig, args=_confget=writeead(tarading.Thr = thre   t0):
     i in range(1r = []
    foads  thres
   fferent usering to dids write 10 threaCreat # 
   ")
    : {e}_id}ser-{userend(f"Uors.app    err        e:
 n ast Exceptio       excep
 eep(0.001)   time.sl            
 , config).set(user_idheac c               i}
 teration':: value, 'ig = {'value' confi          
     ): range(10 in    for i            try:
 :
   alue)d, vnfig(user_i_cof write    de= []
    
rors er    _cache()
_configet_user  cache = g   
  " * 60)
 ("=   printtes")
 ent Wri2: ConcurrTest nt(")
    pri "=" * 60nt("\n" +""
    pri"tionsrate operioncurrent wst c """Te  ites():
 wrurrent_ef test_conc
d
ead safe!")errors - Thro ("✅ Nrint    p else:
    
   rror}") {e"  int(f    pr
        ors[:5]:err error in for:
        errors   if )
 ors)}"errlen(: {✅ Errorsrint(f"}")
    pults)(restions: {len Total operant(f"✅ 
    pri
   n()joi
        t.eads: in thror tds
    frea thr allit fo  
    # Wa
   t.start()      end(t)
 s.app     thread  ))
 s=(i, argig,ead_conf(target=radhreing.T thread =      t:
  in range(10)or i 
    fds = [] threa  ds
 hrea10 tCreate    # 
  {e}")
   hread_id}: "Thread-{td(frs.appenro        er
    ption as e:ce except Ex)
       ep(0.001le  time.s         s")
     id}: Succesthread_"Thread-{ppend(f results.a                 
  nfig: co     if       )
    ide(user_achfrom_cser_config_nfig = get_u         co  
     10): in range(or i  f            try:
 _id):
     (threadonfig_c def read
    []
       errors = []
 esults =
    rser_id = 1   u0)
    
  * 6 print("="ds")
   rent Reaoncur: Cnt("Test 1
    pri=" * 60)n" + "\t(""
    printions""d operareant est concurre  """Ts():
  ent_readcurrest_conf the
)

deg_cacser_confi_u getache,
   ig_cr_confseate_uvalid
    inrom_cache,er_config_f_us
    getrt ( impoheconfig_cacser_s.urvicekend.seacm web.b
fro__))))
(__filebspath(os.path.airnameh.dme(os.patirnath.dt(0, os.papath.insero path
sys.oject root tAdd pr
# xecutor
 ThreadPoolEmportes iturrrent.fu
from concumetiort ng
impt threadios
import  sys
imporport"
im
""tionsrrent operay with concuhread safetest cache t
T"hon3
""nv pyt/usr/bin/e#!