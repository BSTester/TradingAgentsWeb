# ============================================
# 依赖清单说明（重要）
# - 运行时依赖的单一事实源是 `pyproject.toml` 的 [project].dependencies。
# - 本文件为「本地开发全量安装」清单：pyproject 的超集 + 可选项，
#   推荐 `pip install -r requirements.txt` 一次性装齐。
# - 修改运行时依赖时请同步更新 pyproject.toml，保持一致。
# ============================================

# Core dependencies
typing-extensions
setuptools

# LangChain and AI
langchain-openai
langchain-experimental
langchain_anthropic
langchain-google-genai
langgraph

# Data science and analysis
pandas>=1.3.0
numpy>=1.21.0

# Stock data providers
yfinance>=0.2.0          # Yahoo Finance API (全球股票数据)
akshare>=1.9.0           # AKShare (A股、港股、美股数据)
baostock>=0.8.8          # BaoStock (A股专业数据)
tushare                  # Tushare (财经数据)
eodhd                    # EOD Historical Data
finnhub-python           # Finnhub API

# Technical analysis
stockstats>=0.5.0        # Technical indicators (技术指标计算)
backtrader               # Backtesting framework

# Web scraping and parsing
requests>=2.25.0
praw                     # Reddit API
feedparser               # RSS feed parser
parsel                   # Web scraping
beautifulsoup4>=4.9.0    # HTML/XML parsing
lxml>=4.6.0              # XML/HTML parsing
pydantic[email]
# Date and time handling
python-dateutil>=2.8.0
pytz

# Database and storage
chromadb
redis
sqlalchemy[asyncio]>=2.0.0  # ORM for database operations with async support
alembic>=1.7.0           # Database migrations
aiomysql>=0.2.0          # Async MySQL driver (for API routes)
pymysql>=1.0.0           # Sync MySQL driver (for background tasks)
aiosqlite>=0.19.0        # Async SQLite driver (for development/testing)
apscheduler>=3.10.0      # Task scheduling for scheduled analysis tasks

# Web framework and authentication
fastapi>=0.68.0          # Web framework
uvicorn>=0.15.0          # ASGI server
python-multipart>=0.0.5 # Form data parsing
jinja2>=3.0.0            # Template engine
python-jose[cryptography]>=3.3.0  # JWT token handling
passlib[bcrypt]==1.7.4   # Password hashing
bcrypt==4.0.1            # Compatible bcrypt version
python-dotenv>=0.19.0    # Environment variables
markdown>=3.4.0          # Markdown to HTML conversion for email reports

# File handling
openpyxl>=3.0.0          # Excel file support

# UI and CLI
chainlit
rich
questionary
typer>=0.9.0
tqdm
grip>=4.6.2

# Development and testing
pytest>=6.0.0           # Testing framework
pytest-cov>=2.10.0      # Coverage testing
# ta-lib 为可选项：代码内有 try/except 保护（akshare_indicator.py），
# 未安装时自动回退到纯 Python 指标实现。
# 它需要预装 TA-Lib C 库才能通过 pip 安装，会阻塞 Linux 环境的
# `pip install -r requirements.txt`，因此默认注释。
# 如需启用：先安装 C 库（见 https://ta-lib.com/install/）再取消下行注释。
# ta-lib
reportlab>=4.0,<6
